using System;

namespace Inversiones.DS.MapeoABaseDeDatos
{
    /* UNDONE: Estas funcionalidades las está programando otro desarrollador, 
    así que usted solo cuenta con las firmas de las funciones, 
    pero aun así se espera que usted desarrolle y verifique 
    toda la lógica que genera la nueva inversión.
    */
    class RepositorioDeParametros
    {
        public double ObtengaLaTasaDeimpuestoVigente(DateTime laFechaActual)
        {
            throw new NotImplementedException();
        }

        public int ObtengaElConsecutivoParaElCodigoDeReferencia(DateTime laFechaActual)
        {
            throw new NotImplementedException();
        }
    }
}
